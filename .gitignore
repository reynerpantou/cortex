# compiled binary
/cortex
cmd/cortex/cortex

# local database + WAL sidecars
*.db
*.db-shm
*.db-wal
/data

# node
web/node_modules
web/tsconfig.tsbuildinfo

# built SPA (regenerated by `make build`); the placeholder index.html is kept
internal/assets/dist/assets/
internal/assets/dist/*.js
internal/assets/dist/*.css

# env + backups
.env
/backups
